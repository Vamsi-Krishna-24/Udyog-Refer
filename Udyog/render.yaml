# .render.yaml
services:
  - type: web
    name: udyog-refer
    env: python
    rootDir: Udyog
    buildCommand: pip install -r ../requirements.txt && python manage.py collectstatic --noinput
    startCommand: gunicorn Udyog.wsgi:application --bind 0.0.0.0:$PORT
